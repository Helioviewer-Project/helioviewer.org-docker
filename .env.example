# Hostname to use in helioviewer configuration
API_PORT=8081
API_URL=http://127.0.0.1:$API_PORT
CLIENT_PORT=8080
CLIENT_URL=http://127.0.0.1:$CLIENT_PORT
COORDINATOR_PORT=8000
COORDINATOR_URL=http://127.0.0.1:$COORDINATOR_PORT

# Path to existing JPEG2000 files.
# If this path doesn't exist, then it will be created.
HOST_JPEG2000_PATH=$PWD/data/jp2

# Path to cache directory for storing tiles, screenshots, movies, etc.
# If this path doesn't exist, then it will be created.
HOST_CACHE_PATH=$PWD/data/cache

# Path to log directory for storing API logs.
# If this path doesn't exist, then it will be created.
HOST_LOG_PATH=$PWD/data/log

# Passwords. Change to something more secure
DATABASE_ROOT_PASSWORD=helioviewer_root
HV_DB_NAME=helioviewer_name
HV_DB_USER=helioviewer_user
HV_DB_PASS=helioviewer_pass

# These should always be set to the names of the respective services
# from the compose.yaml file.
HV_DB_HOST=database
HV_REDIS_HOST=redis

# To import files from disk, make them accessible at $HOST_JPEG2000_PATH/push
# and run ./manage.sh downloader -d local -b local -m localmove -s "earliest date of your files" -e "latest date of your files"
#    e.g. ./manage.sh downloader -d local -b local -m localmove -s "2021-01-01 00:00:00" -e "2022-01-01 00:00:00"
LOCAL_DATA_DIR=/tmp/jp2/push
