name: 'Run Pytest API Tests'
description: 'Execute pytest tests using ./manage pytest'

inputs:
  working-directory:
    description: 'Working directory containing manage'
    required: false
    default: '.'

runs:
  using: 'composite'
  steps:
    - name: Run pytest tests
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: ./manage pytest
